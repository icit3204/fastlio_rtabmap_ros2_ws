# 回退方案一并恢复方案二记录

记录时间：2026-07-20

## 当前决定

已按要求回退“方案一：WP 点发送 Nav2 目标”，继续使用“方案二：`/plan_nav` 直接替换 `/plan` 控制轮椅跟踪”。

当前控制链路：

```text
/home/dog/plan_nav/main.py
  -> /plan_nav
  -> pure_pursuit_controller_node
  -> /wheelchair_control_command
  -> wheelchair_controller_node
  -> can0
  -> 轮椅
```

离线数据集测试时，如果不启动 `wheelchair_controller_node`，链路只到：

```text
/wheelchair_control_command
```

这是正常的。

## 已回退方案一

### 1. 删除 Nav2 WP 发送器

已删除文件：

```text
/home/dog/plan_nav/core/nav2_goal_sender.py
```

该文件原本负责将 WP 序列逐个发送给 Nav2 `/navigate_to_pose` action。

### 2. 回退 main_window.py

文件：

```text
/home/dog/plan_nav/ui/main_window.py
```

已移除：

```text
_nav2_sender
_start_nav2_waypoint_send()
_stop_nav2_waypoint_send()
_on_nav2_waypoint_finished()
```

规划成功后的操作模式行为已恢复为：

```python
if self._is_operation_mode() and self._plan_publisher:
    self._plan_publisher.set_path(traj)
```

也就是继续更新：

```text
/plan_nav
```

## 已恢复方案二

以下三处已改为 `/plan_nav`。

### 1. pure_pursuit_controller 默认订阅话题

文件：

```text
/home/dog/fastlio_rtabmap_ros2_ws/src/wheelchair_controller/src/pure_pursuit_controller.cpp
```

当前内容：

```cpp
this->declare_parameter("path_topic_name", "/plan_nav");
```

### 2. bringup_2d.launch.py

文件：

```text
/home/dog/fastlio_rtabmap_ros2_ws/src/robot_bringup/launch/bringup_2d.launch.py
```

当前内容：

```python
'path_topic_name': "/plan_nav",
```

### 3. bringup_2d_infra.launch.py

文件：

```text
/home/dog/fastlio_rtabmap_ros2_ws/src/robot_bringup/launch/bringup_2d_infra.launch.py
```

当前内容：

```python
'path_topic_name': "/plan_nav",
```

## 已执行构建

因为修改了 C++ 默认参数，已重新构建：

```bash
cd /home/dog/fastlio_rtabmap_ros2_ws
source /opt/ros/humble/setup.bash
source /home/dog/fastlio_rtabmap_ros2_ws/install/setup.bash
colcon build --packages-select wheelchair_controller --symlink-install
```

构建结果：

```text
Summary: 1 package finished
```

## 已验证

### 1. Python 语法检查

```bash
/usr/bin/python3.10 -m py_compile \
  /home/dog/plan_nav/ui/main_window.py \
  /home/dog/plan_nav/core/nav_publisher.py
```

结果：通过。

### 2. Nav2 WP 发送器已删除

```bash
test ! -e /home/dog/plan_nav/core/nav2_goal_sender.py
```

结果：文件不存在。

### 3. pure_pursuit_controller 已订阅 /plan_nav

短启动验证：

```bash
source /opt/ros/humble/setup.bash
source /home/dog/fastlio_rtabmap_ros2_ws/install/setup.bash
ros2 run wheelchair_controller pure_pursuit_controller_node --ros-args \
  -p use_sim_time:=true
```

检查：

```bash
ros2 node info /pure_pursuit_controller
```

验证结果：

```text
Subscribers:
  /plan_nav: nav_msgs/msg/Path
```

## 使用方式

数据集导航：

```bash
cd /home/dog/fastlio_rtabmap_ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
bash scripts/use_rtabmap_0234_env.sh
ros2 launch robot_bringup bringup_2d.launch.py
```

启动 `main.py`：

```bash
cd /home/dog/plan_nav
source /opt/ros/humble/setup.bash
source /home/dog/fastlio_rtabmap_ros2_ws/install/setup.bash
/usr/bin/python3.10 main.py
```

操作模式下规划路径后，`main.py` 应发布：

```text
/plan_nav
```

验证：

```bash
ros2 node info /pure_pursuit_controller
ros2 topic hz /plan_nav
ros2 topic echo /plan_nav nav_msgs/msg/Path --once
ros2 topic hz /wheelchair_control_command
```

真机控制时再启动：

```bash
ros2 run wheelchair_controller wheelchair_controller_node --ros-args \
  -p auto_start:=true
```

并验证：

```bash
ros2 topic info -v /wheelchair_control_command
```

应看到：

```text
Subscription count: 1
```

## 注意事项

方案二不会自动保留 Nav2 动态避障能力。

当前链路中：

```text
/plan_nav 是 main.py 发布的固定拓扑轨迹
/plan 是 Nav2 基于 costmap 发布的规划路径
```

恢复方案二后，`pure_pursuit_controller_node` 使用 `/plan_nav`，不使用 `/plan`。

真机测试前应确认：

1. `/plan_nav` 在 RViz 中路径正确。
2. 没有重复运行多个 `main.py`。
3. 拓扑边文件没有引用错误的 `edge_N_traj.txt`。
4. 急停可用。
