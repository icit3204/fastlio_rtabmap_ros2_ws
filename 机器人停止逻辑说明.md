# 机器人停止逻辑说明

本文记录当前项目中机器人如何判断停止、如何下发停止命令，以及本次新增的“到达目标点后停止”逻辑。

## 控制链路

当前轮椅控制链路分两层：

```text
pure_pursuit_controller_node
  -> /wheelchair_control_command
  -> wheelchair_controller_node
  -> CAN 或 UDP
  -> 轮椅底盘
```

`pure_pursuit_controller_node` 负责判断路径跟踪、是否到达目标点，并发布控制命令。

`wheelchair_controller_node` 负责把 `/wheelchair_control_command` 转换为底层 CAN 或 UDP 指令，并负责暂停、超时停车、CAN 保持发送。

## 一、pure_pursuit_controller_node 停止判断

文件：

```text
src/wheelchair_controller/src/pure_pursuit_controller.cpp
```

### 1. 相关参数

位置：

```text
pure_pursuit_controller.cpp:20-27
```

当前默认值：

```cpp
lookahead_distance = 1.8
min_turning_radius = 1.5
linear_velocity = 3.0
goal_tolerance = 0.5
goal_yaw_tolerance = 0.1
rotate_in_place_speed = 1.0
stop_on_position_reached = true
path_topic_name = /plan_nav
```

物理含义：

`goal_tolerance`：终点距离容差，单位米。机器人当前位置到路径最后一个点的距离小于该值时，认为“到达目标点位置”。

`goal_yaw_tolerance`：终点朝向容差，单位弧度。只有 `stop_on_position_reached=false` 时才继续用于判断是否需要终点原地调朝向。

`rotate_in_place_speed`：终点原地调朝向速度基准。下发时会乘 `1000`，变成 `mm/s`。

`stop_on_position_reached`：本次新增参数。默认 `true`，表示只要进入 `goal_tolerance` 距离范围就立即停车，不再要求终点朝向对齐。

### 2. launch 中实际覆盖值

使用 `bringup_2d.launch.py` 时，实际参数位置：

```text
src/robot_bringup/launch/bringup_2d.launch.py:376-384
```

当前值：

```python
lookahead_distance = 1.8
min_turning_radius = 1.5
linear_velocity = 3.0
goal_tolerance = 0.9
goal_yaw_tolerance = 3.14
stop_on_position_reached = True
path_topic_name = /plan_nav
```

使用 `bringup_2d_infra.launch.py` 时，实际参数位置：

```text
src/robot_bringup/launch/bringup_2d_infra.launch.py:440-448
```

当前值同上。

因此，常用启动方式下，到达目标点距离判断实际用的是：

```text
goal_tolerance = 0.9 m
stop_on_position_reached = true
```

也就是机器人距离路径最后一个点小于 `0.9 m` 后，会立即进入停止锁存。

### 3. 到达目标点的判断函数

函数：

```text
PurePursuitController::ControlTimerCallback()
```

位置：

```text
pure_pursuit_controller.cpp:87
```

终点距离计算位置：

```text
pure_pursuit_controller.cpp:193-218
```

核心逻辑：

```cpp
dist_to_end = sqrt(dx*dx + dy*dy + dz*dz);
```

其中 `last_pose = path.poses.back().pose`，也就是当前路径消息的最后一个点。

停止判断位置：

```text
pure_pursuit_controller.cpp:220-228
```

核心逻辑：

```cpp
if (dist_to_end < goal_tolerance_) {
    if (stop_on_position_reached_) {
        goal_reached_latched_ = true;
        PublishStopCommand();
        is_stopped_ = true;
        PublishFuturePath(path, current_path_idx_);
        return;
    }
}
```

物理含义：

机器人进入终点距离容差后，马上发布停止命令，并设置 `goal_reached_latched_ = true`。之后控制器不会继续追踪路径，除非收到新的路径。

### 4. 停止锁存

变量：

```text
goal_reached_latched_
```

位置：

```text
pure_pursuit_controller.cpp:427
```

锁存后的处理位置：

```text
pure_pursuit_controller.cpp:106-110
```

核心逻辑：

```cpp
if (goal_reached_latched_) {
    PublishStopCommand();
    PublishFuturePath(path, current_path_idx_);
    return;
}
```

物理含义：

一旦到达目标点，控制器每个周期继续发布 `[0, 0, 0]` 停止命令，防止下一轮定时器又重新计算路径并让机器人继续动。

锁存清除位置：

```text
pure_pursuit_controller.cpp:65-73
```

核心逻辑：

```cpp
void PathCallback(...) {
    path_ = *msg;
    path_updated_ = true;
    goal_reached_latched_ = false;
}
```

物理含义：

收到新路径后，认为这是新的导航任务，解除终点停止锁存。

### 5. 停止命令发布函数

函数：

```text
PurePursuitController::PublishStopCommand()
```

位置：

```text
pure_pursuit_controller.cpp:382-389
```

下发内容：

```cpp
stop_msg.data[0] = 0.0;  // radius
stop_msg.data[1] = 0.0;  // velocity
stop_msg.data[2] = 0.0;  // distance
pub_control_->publish(stop_msg);
```

物理含义：

`/wheelchair_control_command` 使用 `std_msgs/msg/Float32MultiArray`：

```text
data[0] = radius_mm
data[1] = velocity_mm_s
data[2] = distance_mm
```

停止命令就是：

```text
[0.0, 0.0, 0.0]
```

### 6. 旧的终点原地调朝向逻辑

位置：

```text
pure_pursuit_controller.cpp:228-245
```

只有当：

```text
stop_on_position_reached = false
```

并且：

```text
dist_to_end < goal_tolerance
abs(yaw_error) > goal_yaw_tolerance
```

才会执行原地调朝向。

下发逻辑：

```cpp
align_msg.data[0] = 0.0;
align_msg.data[1] = ±rotate_in_place_speed * 1000.0;
align_msg.data[2] = 0.0;
```

当前常用 launch 已设置：

```text
stop_on_position_reached = True
```

所以到达目标点后不会再进入这段原地调朝向逻辑。

### 7. 没有路径或 TF 失败时的行为

没有路径：

```text
pure_pursuit_controller.cpp:93-96
```

当前行为是直接 `return`，不会主动发布停止命令。

TF 查询失败：

```text
pure_pursuit_controller.cpp:119-133
```

当前行为也是直接 `return`，不会主动发布停止命令。

实际底层是否停下，依赖 `wheelchair_controller_node` 的 `command_timeout_ms` 超时停车。

## 二、wheelchair_controller_node 如何使底盘停止

文件：

```text
src/wheelchair_controller/src/wheelchair_controller_node.cpp
```

### 1. 相关参数

位置：

```text
wheelchair_controller_node.cpp:25-40
```

默认值：

```cpp
auto_start = false
min_command_interval_ms = 20.0
output_transport = can
can_velocity_limit = 16380
can_wheel_half_track_mm = 300.0
can_straight_radius_threshold_mm = 10000.0
can_min_turn_radius_mm = 1000.0
can_invert_radius = true
can_send_period_ms = 20.0
command_timeout_ms = 500.0
can_use_command_distance = false
can_default_distance = 0
```

参数文件位置：

```text
src/wheelchair_controller/config/wheelchair_controller_param.yaml:1-20
```

常用配置值同上。

物理含义：

`auto_start`：是否启动后立即允许控制。`false` 时节点启动后处于暂停，需要在该节点终端按空格启动。

`min_command_interval_ms`：接收 `/wheelchair_control_command` 后立即发送底层命令的最小间隔，当前 `20 ms`。

`can_send_period_ms`：CAN 保持帧发送周期，当前 `20 ms`。即最近一条命令会被周期性保持发送。

`command_timeout_ms`：超过该时间没有新命令，发送一次停止帧，当前 `500 ms`。

`can_velocity_limit`：左右轮 int16 速度限幅，当前 `16380`。

### 2. 接收上层命令

订阅话题：

```text
/wheelchair_control_command
```

位置：

```text
wheelchair_controller_node.cpp:60-62
```

回调函数：

```text
WheelchairController::CommandCallback()
```

位置：

```text
wheelchair_controller_node.cpp:158-181
```

暂停状态下的停止逻辑：

```cpp
if (control_paused_) {
    SendControlMsg(0.0, 0.0, 0.0);
    return;
}
```

物理含义：

如果底层控制节点处于暂停状态，即使上层发来运动命令，也会强制转成停止命令。

### 3. 停止命令进入底层发送函数

函数：

```text
WheelchairController::SendControlMsg()
```

位置：

```text
wheelchair_controller_node.cpp:183-199
```

作用：

保存最近命令，并根据 `output_transport` 发送 UDP 或 CAN。

停止命令：

```cpp
SendControlMsg(0.0, 0.0, 0.0);
```

会进一步进入：

```text
SendCanFrame(0.0, 0.0, 0.0)
```

或者：

```text
SendUdpMsg(0.0, 0.0)
```

### 4. CAN 层如何变成左右轮停止

函数：

```text
WheelchairController::SendCanFrame()
```

位置：

```text
wheelchair_controller_node.cpp:262-311
```

停止判断：

```text
wheelchair_controller_node.cpp:273-276
```

核心逻辑：

```cpp
if (vel == 0) {
    v_left = 0;
    v_right = 0;
}
```

物理含义：

只要上层命令中的 `velocity` 为 `0`，底层左右轮速度都会被设为 `0`。随后通过 CAN 帧发出。

CAN 帧写入位置：

```text
wheelchair_controller_node.cpp:294-302
```

左右轮数据：

```cpp
PutInt16LE(frame.data, 0, v_left);
PutInt16LE(frame.data, 2, v_right);
```

停止帧对应：

```text
v_left = 0
v_right = 0
distance = 0
```

### 5. 超时停车

函数：

```text
WheelchairController::CanTimerCallback()
```

位置：

```text
wheelchair_controller_node.cpp:226-260
```

核心判断：

```cpp
elapsed_ms <= command_timeout_ms_ && !control_paused_
```

如果超过 `command_timeout_ms`，进入：

```cpp
else if (!sent_timeout_stop_) {
    sent_timeout_stop_ = true;
    should_send = true;
}
```

因为此时局部变量默认就是：

```cpp
radius = 0.0;
velocity = 0.0;
distance = 0.0;
```

所以最终会发送：

```text
SendCanFrame(0.0, 0.0, 0.0)
```

物理含义：

如果上层控制器卡住、不再发布 `/wheelchair_control_command`，底层 CAN 节点会在 `500 ms` 后发送一次停止帧。

### 6. 空格暂停停车

函数：

```text
WheelchairController::KeyboardListener()
```

位置：

```text
wheelchair_controller_node.cpp:338-366
```

核心逻辑：

```cpp
if (c == ' ') {
    control_paused_ = !control_paused_;
    if (control_paused_) {
        SendControlMsg(0.0, 0.0, 0.0);
    }
}
```

物理含义：

在 `wheelchair_controller_node` 所在终端按空格，可以切换暂停/恢复。进入暂停时立即发送停止命令。

### 7. 节点析构停车

析构函数：

```text
WheelchairController::~WheelchairController()
```

位置：

```text
wheelchair_controller_node.cpp:82-94
```

核心逻辑：

```cpp
if (can_sockfd_ >= 0) {
    SendCanFrame(0.0, 0.0, 0.0);
    close(can_sockfd_);
}
```

物理含义：

节点退出并关闭 CAN 前，会发送一次停止帧。

## 三、当前到点停车的推荐配置

数据集或真机使用 `bringup_2d.launch.py` / `bringup_2d_infra.launch.py` 时，推荐保持：

```python
'goal_tolerance': 0.9,
'goal_yaw_tolerance': 3.14,
'stop_on_position_reached': True,
```

含义：

机器人距离目标点小于 `0.9 m` 后立即停车，并持续发布停止命令。

如果希望“先到位置，再原地旋转对齐终点朝向”，可以改成：

```python
'stop_on_position_reached': False,
'goal_yaw_tolerance': 0.2,
```

但当前需求是“到达目标点后停止”，所以应保持：

```text
stop_on_position_reached = true
```

## 四、验证命令

确认纯跟踪控制节点参数：

```bash
ros2 param get /pure_pursuit_controller goal_tolerance
ros2 param get /pure_pursuit_controller stop_on_position_reached
ros2 param get /pure_pursuit_controller path_topic_name
```

确认停止命令是否发出：

```bash
ros2 topic echo /wheelchair_control_command
```

到达目标点后应持续看到：

```text
data:
- 0.0
- 0.0
- 0.0
```

确认底层控制节点订阅：

```bash
ros2 topic info -v /wheelchair_control_command
```

确认 CAN 节点超时停车参数：

```bash
ros2 param get /wheelchair_controller command_timeout_ms
ros2 param get /wheelchair_controller can_send_period_ms
ros2 param get /wheelchair_controller auto_start
```

## 五、注意事项

`pure_pursuit_controller_node` 没有路径或 TF 查询失败时，目前不会主动发停止，只会退出本轮控制循环。这种情况下依赖 `wheelchair_controller_node` 的 `command_timeout_ms = 500.0 ms` 超时停车。

`bringup_2d.launch.py` 和 `bringup_2d_infra.launch.py` 中定义了 `collision_monitor`，但当前文件底部对应的 `ld.add_action(collision_monitor)` 和生命周期管理器是注释状态，所以这些 launch 当前不会真正启动 `collision_monitor`。到点停车不依赖 `collision_monitor`，但近距离障碍急停也不会由这两个 launch 自动生效。
