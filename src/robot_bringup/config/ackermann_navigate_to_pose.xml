<!--
  Current development MK-mini NavigateToPose policy.

  Motion authority is exclusively FollowPath -> controller_server. Recovery
  is limited to replanning and costmap clearing. Spin, BackUp,
  DriveOnHeading, AssistedTeleop, and Wait behavior actions are intentionally
  absent because this chassis is forward-only Ackermann and behavior_server's
  Humble cmd_vel output does not own the canonical Collision Monitor input.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="3" name="AckermannNavigateRecovery">
      <PipelineSequence name="NavigateWithForwardAckermannReplanning">
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{path}" controller_id="FollowPath" goal_checker_id="general_goal_checker"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>
      <ReactiveFallback name="NonMotionRecoveryFallback">
        <GoalUpdated/>
        <Sequence name="ClearCostmapsOnly">
          <ClearEntireCostmap name="ClearLocalCostmap-Recovery" service_name="local_costmap/clear_entirely_local_costmap"/>
          <ClearEntireCostmap name="ClearGlobalCostmap-Recovery" service_name="global_costmap/clear_entirely_global_costmap"/>
        </Sequence>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
