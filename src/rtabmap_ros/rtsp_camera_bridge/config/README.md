# Camera calibration (optional)

Place `camera_info` YAML here and pass `camera_info_url:=file://$(pkg-path rtsp_camera_bridge)/config/your_camera.yaml` if needed.
Standard format: image_width, image_height, camera_matrix, distortion_coefficients, etc.
