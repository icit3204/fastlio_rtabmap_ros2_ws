# 方案一 WP 点发送 Nav2 目标修改记录

记录时间：2026-07-20

## 修改目标

回退“方案二”的 `/plan_nav` 直接替换 `/plan`，改为“方案一”：

```text
main.py 负责拓扑 WP 路线
Nav2 负责每段实时规划和避障
pure_pursuit_controller 继续订阅 Nav2 的 /plan
```

最终控制链路：

```text
/home/dog/plan_nav/main.py
  -> Nav2 /navigate_to_pose action
  -> Nav2 planner 输出 /plan
  -> pure_pursuit_controller_node
  -> /wheelchair_control_command
  -> wheelchair_controller_node
  -> can0
  -> 轮椅
```

## 已回退方案二

以下三处已从 `/plan_nav` 改回 `/plan`。

### 1. pure_pursuit_controller 默认参数

文件：

```text
/home/dog/fastlio_rtabmap_ros2_ws/src/wheelchair_controller/src/pure_pursuit_controller.cpp
```

当前内容：

```cpp
this->declare_parameter("path_topic_name", "/plan");
```

### 2. bringup_2d.launch.py

文件：

```text
/home/dog/fastlio_rtabmap_ros2_ws/src/robot_bringup/launch/bringup_2d.launch.py
```

当前内容：

```python
'path_topic_name': "/plan",
```

### 3. bringup_2d_infra.launch.py

文件：

```text
/home/dog/fastlio_rtabmap_ros2_ws/src/robot_bringup/launch/bringup_2d_infra.launch.py
```

当前内容：

```python
'path_topic_name': "/plan",
```

## 方案一新增修改

### 1. 新增 Nav2 WP 发送线程

新增文件：

```text
/home/dog/plan_nav/core/nav2_goal_sender.py
```

作用：

```text
把拓扑规划得到的 WP 节点序列，逐个发送给 Nav2 的 /navigate_to_pose action。
```

它发送的是 `nav2_msgs/action/NavigateToPose`，等当前 WP 成功到达后，再发送下一个 WP。

这样每一段路径都由 Nav2 根据实时 costmap 规划，Nav2 会继续发布：

```text
/plan
```

### 2. 修改 main_window.py 的规划成功处理

修改文件：

```text
/home/dog/plan_nav/ui/main_window.py
```

核心变化：

```text
调试模式：
  继续保留原来的轨迹绘制和 UDP 发送预览逻辑。

操作模式：
  不再使用 /plan_nav 直接驱动轮椅。
  规划成功后，调用 Nav2WaypointSender，把 WP 序列逐个发给 Nav2。
```

`/plan_nav` 仍可作为观测路径发布，但不再作为轮椅控制输入。

## 行为变化

旧方案二：

```text
main.py /plan_nav
  -> pure_pursuit_controller
  -> 轮椅
```

当前方案一：

```text
main.py WP 序列
  -> Nav2 /navigate_to_pose
  -> Nav2 /plan
  -> pure_pursuit_controller
  -> 轮椅
```

因此：

```text
pure_pursuit_controller 必须订阅 /plan
不要订阅 /plan_nav
```

## 已执行构建

因为回退了 C++ 默认参数，已重新构建：

```bash
cd /home/dog/fastlio_rtabmap_ros2_ws
source /opt/ros/humble/setup.bash
source /home/dog/fastlio_rtabmap_ros2_ws/install/setup.bash
colcon build --packages-select wheelchair_controller --symlink-install
```

构建结果：

```text
Summary: 1 package finished
```

## 已执行静态验证

Python 语法检查：

```bash
/usr/bin/python3.10 -m py_compile \
  /home/dog/plan_nav/core/nav2_goal_sender.py \
  /home/dog/plan_nav/ui/main_window.py
```

结果：通过。

确认 Nav2 发送器可导入：

```bash
/usr/bin/python3.10 - <<'PY'
import sys
sys.path.insert(0, "/home/dog/plan_nav")
from core.nav2_goal_sender import Nav2WaypointSender
print(Nav2WaypointSender.__name__)
PY
```

结果：

```text
Nav2WaypointSender
```

确认 `pure_pursuit_controller` 已订阅 `/plan`：

```bash
ros2 node info /pure_pursuit_controller
```

验证时看到：

```text
Subscribers:
  /plan: nav_msgs/msg/Path
```

## 运行要求

`/home/dog/plan_nav/main.py` 必须使用系统 Python 3.10 运行，避免 Conda Python 3.13 导致 ROS2 `rclpy` 导入失败。

推荐启动：

```bash
cd /home/dog/plan_nav
source /opt/ros/humble/setup.bash
source /home/dog/fastlio_rtabmap_ros2_ws/install/setup.bash
/usr/bin/python3.10 main.py
```

不要使用当前 Conda 的：

```text
/home/dog/miniforge3/bin/python3
```

该 Python 是 3.13，导入 Humble 的 `rclpy` 会失败。

## 使用流程

1. 启动数据集导航或真机导航。

数据集导航示例：

```bash
cd /home/dog/fastlio_rtabmap_ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
bash scripts/use_rtabmap_0234_env.sh
ros2 launch robot_bringup bringup_2d.launch.py
```

真机导航示例：

```bash
cd /home/dog/fastlio_rtabmap_ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
bash scripts/use_rtabmap_0234_env.sh
ros2 launch robot_bringup bringup_2d.launch.py start_livox:=true start_realsense:=true use_sim_time:=false
```

2. 启动底层轮椅控制节点。

```bash
ros2 run wheelchair_controller wheelchair_controller_node --ros-args \
  -p auto_start:=true
```

3. 启动 `main.py`。

```bash
cd /home/dog/plan_nav
source /opt/ros/humble/setup.bash
source /home/dog/fastlio_rtabmap_ros2_ws/install/setup.bash
/usr/bin/python3.10 main.py
```

4. 在 `main.py` 中切换到操作模式。

5. 在规划模式下选择起点 WP 和终点 WP。

6. `main.py` 会计算拓扑 WP 序列，并逐个发送给 Nav2。

## 验证命令

确认 Nav2 action server 存在：

```bash
ros2 action list -t | grep navigate_to_pose
```

应看到：

```text
/navigate_to_pose [nav2_msgs/action/NavigateToPose]
```

确认 Nav2 正在输出 `/plan`：

```bash
ros2 topic hz /plan
ros2 topic echo /plan nav_msgs/msg/Path --once
```

确认 pure pursuit 订阅 `/plan`：

```bash
ros2 node info /pure_pursuit_controller
```

应看到：

```text
/plan: nav_msgs/msg/Path
```

确认产生轮椅控制命令：

```bash
ros2 topic hz /wheelchair_control_command
ros2 topic echo /wheelchair_control_command
```

确认底层控制节点接收命令：

```bash
ros2 topic info -v /wheelchair_control_command
```

应看到：

```text
Subscription count: 1
```

## 回退方案一

如果需要取消本次方案一修改：

1. 删除或不再使用：

```text
/home/dog/plan_nav/core/nav2_goal_sender.py
```

2. 将 `/home/dog/plan_nav/ui/main_window.py` 中操作模式下调用 `_start_nav2_waypoint_send(path_nodes)` 的逻辑移除，恢复旧的 `_start_udp_send(traj)` 或 `/plan_nav` 直接发布逻辑。

3. 如果要重新回到方案二，还需要把 `pure_pursuit_controller` 的 `path_topic_name` 再改成 `/plan_nav`，并重新构建 `wheelchair_controller`。

## 风险和注意事项

1. 当前实现是逐个 WP 发送 `NavigateToPose`，每到达一个 WP 后再发送下一个 WP。机器人可能在 WP 附近有短暂停顿。

2. 后续可优化为 `NavigateThroughPoses`，一次性发送多个途经点，使运行更连续。

3. 方案一保留的是 Nav2 基于 costmap 的规划避障能力。近距离强制急停仍建议单独接入 `collision_monitor` 或 safety gate。

4. 如果拓扑 WP 本身放在不可达位置，Nav2 会拒绝目标或执行失败。
