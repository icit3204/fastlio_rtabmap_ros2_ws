# 单线雷达避障接入方案与修改记录

日期：2026-07-22

## 目标

在现有 `/home/dog/plan_nav/main.py` 发布 `/plan_nav` 的拓扑路径导航基础上，接入轮椅底部中央的单线雷达进行临时避障。

保持 `/plan_nav` 为原始路径，只读、不改写、不插入临时避障点。避障节点根据 `/scan` 生成临时实际跟踪路径 `/active_plan`，避障结束后回到原 `/plan_nav` 后续路径。

## 修改后的话题链路

```text
/home/dog/plan_nav/main.py
  -> /plan_nav                         # 原始拓扑路径，只读

plan_nav_laser_avoidance.py
  subscribe /plan_nav
  subscribe /scan
  lookup map -> base_link
  -> /active_plan                      # 实际给 pure pursuit 跟踪的路径
  -> /laser_avoidance_state
  -> /laser_avoidance_return_point

pure_pursuit_controller_node
  subscribe /active_plan
  -> /wheelchair_control_command_raw

laser_command_safety_filter.py
  subscribe /wheelchair_control_command_raw
  subscribe /scan
  -> /wheelchair_control_command
  -> /laser_safety_state

wheelchair_controller_node
  subscribe /wheelchair_control_command
  -> CAN
```

关闭避障时：

```text
/plan_nav -> pure_pursuit_controller_node -> /wheelchair_control_command -> wheelchair_controller_node
```

## 如何保证 /plan_nav 只读

1. 新增避障节点只订阅 `/plan_nav`，不发布 `/plan_nav`。
2. 新增避障节点发布的是派生路径 `/active_plan`。
3. pure pursuit 在启用避障时订阅 `/active_plan`，不直接订阅 `/plan_nav`。
4. `/plan_nav` 仍由 `/home/dog/plan_nav/main.py` 持续发布。

验证：

```bash
ros2 topic info -v /plan_nav
```

期望：

- Publishers 中只有 plan_nav 的 `plan_publisher`。
- `plan_nav_laser_avoidance` 只出现在 Subscribers 中。

## 避障触发参数

当前参数写在：

- `src/robot_bringup/launch/bringup_2d.launch.py`
- `src/robot_bringup/launch/bringup_2d_infra.launch.py`

主要参数：

```text
front_sector_deg: 25.0
obstacle_enter_distance: 3.0 m
obstacle_exit_distance: 3.2 m
emergency_stop_distance: 0.9 m
obstacle_confirm_frames: 3
side_inner_deg: 25.0
side_outer_deg: 100.0
side_clearance_min: 1.2 m
lateral_offset: 1.0 m
return_margin: 2.0 m
min_return_distance: 2.5 m
max_return_distance: 6.0 m
clear_duration: 1.0 s
return_tolerance: 0.55 m
path_lateral_tolerance: 0.55 m
heading_tolerance_deg: 30.0
```

含义：

- 前方 `+-25°` 内连续 3 帧检测到 3m 内障碍，开始避障判断。
- 0.9m 内障碍由安全过滤器直接停车。
- 左右侧 `25°~100°` 范围比较空旷程度，选择更空一侧绕行。
- 左右都不满足 `1.2m` 侧向空间时，不绕行，发布当前位置保持路径，并由安全过滤器负责停车/限速。

## 临时弧线路径设计

触发避障后，节点内部生成：

```text
当前机器人位置
  -> 贝塞尔弧线控制点1
  -> 贝塞尔弧线控制点2
  -> P_return
  -> 原 /plan_nav 中 P_return 后面的路径点
```

`P_leave` 不作为必须到达点保存，它等价于当前机器人在 `/plan_nav` 上的最近点附近。避障发生时，节点会从当前位姿直接生成临时弧线。

`P_return` 是原 `/plan_nav` 上的点：

```text
P_return = 当前最近路径点之后 obstacle_distance + return_margin 的原路径点
```

并限制在：

```text
min_return_distance <= 回归距离 <= max_return_distance
```

也就是默认大约选择障碍物后方 2m 左右的原路径点，且总回归距离限制在 2.5m 到 6.0m 之间。

## P_leave 到 P_return 之间的原路径点如何处理

这些点不会删除，也不会写回 `/plan_nav`。

生成 `/active_plan` 时，`P_leave` 到 `P_return` 之间被临时跳过：

```text
原 /plan_nav:
A -> B -> P_leave -> p1 -> p2 -> p3 -> P_return -> C -> D

避障期间 /active_plan:
当前位姿 -> 临时弧线 -> P_return -> C -> D
```

这样机器人绕过障碍后不再补走 `p1/p2/p3`，而是从 `P_return` 继续跟随原始路径后半段。

## 避障结束条件

同时满足：

1. 前方障碍距离大于 `obstacle_exit_distance`。
2. 前方清空持续超过 `clear_duration`。
3. 机器人距离 `P_return` 小于 `return_tolerance`。
4. 机器人到原 `/plan_nav` 最近点距离小于 `path_lateral_tolerance`。
5. 当前朝向与原路径切线方向差小于 `heading_tolerance_deg`。

满足后，`/active_plan` 切回透传 `/plan_nav`，机器人继续走原拓扑路径后续点。

## 修改文件

新增：

- `src/robot_bringup/scripts/plan_nav_laser_avoidance.py`
- `src/robot_bringup/scripts/laser_command_safety_filter.py`
- `单线雷达避障接入方案与修改记录_20260722.md`

修改：

- `src/robot_bringup/CMakeLists.txt`
- `src/wheelchair_controller/src/pure_pursuit_controller.cpp`
- `src/robot_bringup/launch/bringup_2d.launch.py`
- `src/robot_bringup/launch/bringup_2d_infra.launch.py`

## 启动方式

真机导航，启用避障：

```bash
cd /home/dog/fastlio_rtabmap_ros2_ws
source install/setup.bash
ros2 launch robot_bringup bringup_2d.launch.py \
  start_livox:=true \
  start_realsense:=true \
  use_sim_time:=false \
  enable_laser_avoidance:=true \
  scan_topic:=/scan
```

关闭避障，回到原 `/plan_nav` 直接控制：

```bash
ros2 launch robot_bringup bringup_2d.launch.py \
  start_livox:=true \
  start_realsense:=true \
  use_sim_time:=false \
  enable_laser_avoidance:=false enable_rviz:=false
```

## 验证命令

确认 `/plan_nav` 只读：

```bash
ros2 topic info -v /plan_nav
```

确认实际跟踪路径：

```bash
ros2 topic hz /active_plan
ros2 topic echo --once /laser_avoidance_state
```

确认 pure pursuit 订阅 `/active_plan`：

```bash
ros2 node info /pure_pursuit_controller
```

确认控制过滤链路：

```bash
ros2 topic hz /wheelchair_control_command_raw
ros2 topic hz /wheelchair_control_command
ros2 topic echo --once /laser_safety_state
```

确认单线雷达：

```bash
ros2 topic echo --qos-profile sensor_data --once /scan sensor_msgs/msg/LaserScan
```

## 回退方法

保留源码但启动时关闭避障：

```bash
enable_laser_avoidance:=false
```

完全回退代码时，删除新增脚本和本文档，并恢复以下改动：

1. `src/robot_bringup/CMakeLists.txt` 中移除：

```text
scripts/plan_nav_laser_avoidance.py
scripts/laser_command_safety_filter.py
```

2. `src/wheelchair_controller/src/pure_pursuit_controller.cpp` 中移除 `control_topic_name` 参数，恢复直接发布 `/wheelchair_control_command`。

3. 两个 launch 中恢复 pure pursuit：

```python
'path_topic_name': "/plan_nav",
'control_topic_name': "/wheelchair_control_command",
```

并删除 `plan_nav_laser_avoidance` 与 `laser_command_safety_filter` 两个 Node。

4. 重新编译：

```bash
colcon build --packages-select wheelchair_controller robot_bringup --symlink-install
source install/setup.bash
```

## 已执行验证

```bash
/usr/bin/python3 -m py_compile \
  src/robot_bringup/scripts/plan_nav_laser_avoidance.py \
  src/robot_bringup/scripts/laser_command_safety_filter.py \
  src/robot_bringup/launch/bringup_2d.launch.py \
  src/robot_bringup/launch/bringup_2d_infra.launch.py

colcon build --packages-select wheelchair_controller robot_bringup --symlink-install

ros2 run robot_bringup plan_nav_laser_avoidance.py --ros-args -p enabled:=false
ros2 run robot_bringup laser_command_safety_filter.py --ros-args -p enabled:=false
ros2 launch robot_bringup bringup_2d.launch.py --show-args
```

结果：

- Python 语法检查通过。
- 两个包编译通过。
- 两个新增节点可被 `ros2 run` 启动。
- `bringup_2d.launch.py` 可解析，并显示新增参数 `enable_laser_avoidance` 与 `scan_topic`。
